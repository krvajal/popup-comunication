# Cross-Origin comunication between `Window` objects
This demo apps shows how you can comunicate with a popup using the `window.postMessage` api.
The most common scenario this can be used is for authentication with social services like Facebook.
